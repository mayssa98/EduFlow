kind = "web"
previewPath = "/"
title = "EduFlow"
version = "1.0.0"
id = "artifacts/eduflow-frontend"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26151

[services.development]
run = "pnpm --filter @workspace/eduflow-frontend run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/eduflow-frontend", "run", "build" ]
publicDir = "artifacts/eduflow-frontend/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26151"
BASE_PATH = "/"
