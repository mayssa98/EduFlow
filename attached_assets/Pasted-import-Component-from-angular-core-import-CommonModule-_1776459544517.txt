import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';
import { FormsModule, ReactiveFormsModule, FormBuilder, FormGroup, Validators } from '@angular/forms';

@Component({
  selector: 'app-auth-page',
  standalone: true,
  imports: [CommonModule, FormsModule, ReactiveFormsModule],
  template: `
    <div class="auth-wrapper">
      <div class="container" [class.right-panel-active]="isSignUpMode">
        
        <!-- Formulaire d'inscription -->
        <div class="form-container sign-up-container">
          <form [formGroup]="signUpForm" (ngSubmit)="onSignUp()">
            <h1>Créer un compte</h1>
            <div class="social-container">
              <a href="#" class="social">G</a> <!-- Vous pouvez mettre l'icône de Google ici via FontAwesome/SVG -->
            </div>
            <span>ou utilisez votre email</span>
            <input type="text" placeholder="Nom complet" formControlName="name" />
            <input type="email" placeholder="Adresse Email" formControlName="email" />
            <input type="password" placeholder="Mot de passe" formControlName="password" />
            <button type="submit" [disabled]="signUpForm.invalid">S'inscrire</button>
          </form>
        </div>

        <!-- Formulaire de Connexion -->
        <div class="form-container sign-in-container">
          <form [formGroup]="signInForm" (ngSubmit)="onSignIn()">
            <h1>Se connecter</h1>
            <div class="social-container">
              <a href="#" class="social">G</a> <!-- Icône Google -->
            </div>
            <span>ou utilisez votre compte existant</span>
            <input type="email" placeholder="Adresse Email" formControlName="email" />
            <input type="password" placeholder="Mot de passe" formControlName="password" />
            <a href="#" class="forgot-password">Mot de passe oublié ?</a>
            <button type="submit" [disabled]="signInForm.invalid">Connexion</button>
          </form>
        </div>

        <!-- Panneau de Transition (Glissant) -->
        <div class="overlay-container">
          <div class="overlay">
            <div class="overlay-panel overlay-left">
              <h1>De retour !</h1>
              <p>Pour rester connecté avec nous, veuillez vous connecter avec vos informations de compte</p>
              <button class="ghost" (click)="toggleMode()">Se connecter</button>
            </div>
            <div class="overlay-panel overlay-right">
              <h1>Bonjour, Étudiant !</h1>
              <p>Entrez vos coordonnées et commencez votre voyage éducatif avec EduFlow</p>
              <button class="ghost" (click)="toggleMode()">S'inscrire</button>
            </div>
          </div>
        </div>

      </div>
    </div>
  `,
  styles: [`
    @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700&display=swap');

    .auth-wrapper {
      background: #f1f5f9; /* Doux et moderne */
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      font-family: 'Outfit', sans-serif;
      height: 100vh;
      width: 100vw;
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    h1 {
      font-weight: 700;
      margin: 0;
      color: #1e293b;
    }

    p {
      font-size: 15px;
      font-weight: 300;
      line-height: 24px;
      letter-spacing: 0.5px;
      margin: 20px 0 30px;
    }

    span {
      font-size: 13px;
      color: #64748b;
      margin-bottom: 20px;
    }

    a {
      color: #3b82f6;
      font-size: 14px;
      text-decoration: none;
      margin: 15px 0;
      transition: color 0.3s ease;
    }
    a:hover { color: #2563eb; }

    button {
      border-radius: 25px;
      border: 1px solid #4F46E5; /* Indigo premium */
      background-color: #4F46E5;
      color: #FFFFFF;
      font-size: 13px;
      font-weight: bold;
      padding: 12px 50px;
      letter-spacing: 1px;
      text-transform: uppercase;
      transition: transform 80ms ease-in, background-color 0.3s;
      cursor: pointer;
      box-shadow: 0 4px 14px 0 rgba(79, 70, 229, 0.39);
    }

    button:active { transform: scale(0.95); }
    button:focus { outline: none; }
    button:hover { background-color: #4338ca; }

    button.ghost {
      background-color: transparent;
      border-color: #FFFFFF;
      box-shadow: none;
    }
    button.ghost:hover {
      background-color: rgba(255,255,255, 0.1);
    }

    button:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }

    form {
      background-color: #FFFFFF;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      padding: 0 50px;
      height: 100%;
      text-align: center;
    }

    input {
      background-color: #f8fafc;
      border: 1px solid #e2e8f0;
      padding: 14px 15px;
      margin: 8px 0;
      width: 100%;
      border-radius: 12px;
      font-family: 'Outfit', sans-serif;
      outline: none;
      transition: all 0.3s;
    }
    input:focus {
       background-color: #ffffff;
       border: 1px solid #4F46E5;
       box-shadow: 0 0 0 3px rgba(79, 70, 229, 0.1);
    }

    /* Le conteneur principal gérant l'animation */
    .container {
      background-color: #ffffff;
      border-radius: 24px;
      box-shadow: 0 15px 35px rgba(0,0,0,0.1), 0 5px 15px rgba(0,0,0,0.05);
      position: relative;
      overflow: hidden;
      width: 900px;
      max-width: 95%;
      min-height: 600px;
    }

    .form-container {
      position: absolute;
      top: 0;
      height: 100%;
      transition: all 0.6s ease-in-out;
    }

    .sign-in-container {
      left: 0;
      width: 50%;
      z-index: 2;
    }

    .container.right-panel-active .sign-in-container {
      transform: translateX(100%);
    }

    .sign-up-container {
      left: 0;
      width: 50%;
      opacity: 0;
      z-index: 1;
    }

    .container.right-panel-active .sign-up-container {
      transform: translateX(100%);
      opacity: 1;
      z-index: 5;
      animation: show 0.6s;
    }

    @keyframes show {
      0%, 49.99% { opacity: 0; z-index: 1; }
      50%, 100% { opacity: 1; z-index: 5; }
    }

    /* La partie du panneau coloré (Overlay) */
    .overlay-container {
      position: absolute;
      top: 0;
      left: 50%;
      width: 50%;
      height: 100%;
      overflow: hidden;
      transition: transform 0.6s ease-in-out;
      z-index: 100;
    }

    .container.right-panel-active .overlay-container {
      transform: translateX(-100%);
    }

    .overlay {
      /* Dégradé Indigo Premium */
      background: linear-gradient(135deg, #6366F1, #4F46E5);
      background-repeat: no-repeat;
      background-size: cover;
      background-position: 0 0;
      color: #FFFFFF;
      position: relative;
      left: -100%;
      height: 100%;
      width: 200%;
      transform: translateX(0);
      transition: transform 0.6s ease-in-out;
    }

    .container.right-panel-active .overlay {
      transform: translateX(50%);
    }

    .overlay-panel {
      position: absolute;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      padding: 0 40px;
      text-align: center;
      top: 0;
      height: 100%;
      width: 50%;
      transform: translateX(0);
      transition: transform 0.6s ease-in-out;
    }

    .overlay-left { transform: translateX(-20%); }
    .container.right-panel-active .overlay-left { transform: translateX(0); }
    .overlay-right { right: 0; transform: translateX(0); }
    .container.right-panel-active .overlay-right { transform: translateX(20%); }

    /* Icônes Sociaux */
    .social-container {
      margin: 20px 0;
      display: flex;
      gap: 15px;
    }

    .social-container a {
      border: 1px solid #e2e8f0;
      border-radius: 50%;
      display: inline-flex;
      justify-content: center;
      align-items: center;
      margin: 0;
      height: 45px;
      width: 45px;
      font-weight: bold;
      color: #1e293b;
      transition: all 0.3s ease;
    }
    
    .social-container a:hover {
       background-color: #f8fafc;
       border-color: #4F46E5;
       color: #4F46E5;
       transform: translateY(-2px);
       box-shadow: 0 4px 10px rgba(0,0,0,0.05);
    }
  `]
})
export class AuthPageComponent {
  // Détermine si on est sur la vue 'Inscription' ou 'Connexion'
  isSignUpMode = false;
  
  signInForm: FormGroup;
  signUpForm: FormGroup;

  constructor(private fb: FormBuilder) {
    this.signInForm = this.fb.group({
      email: ['', [Validators.required, Validators.email]],
      password: ['', Validators.required]
    });

    this.signUpForm = this.fb.group({
      name: ['', Validators.required],
      email: ['', [Validators.required, Validators.email]],
      password: ['', Validators.required]
    });
  }

  // Animation : Basculer entre Login et Inscription
  toggleMode() {
    this.isSignUpMode = !this.isSignUpMode;
  }

  // Logique du bouton Login
  onSignIn() {
    if (this.signInForm.valid) {
      console.log('Tentative de connexion :', this.signInForm.value);
      // Appelez votre vrai service API pour le Login ici
    }
  }

  // Logique du bouton Inscription
  onSignUp() {
    if (this.signUpForm.valid) {
      console.log('Tentative d\'inscription :', this.signUpForm.value);
      // Appelez votre vrai service API pour le Signup ici
    }
  }
}
